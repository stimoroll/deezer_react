const search = (search) => `https://proxy.kpoplawski.repl.co/search/${search}`;
const searchex = (search) => `https://proxy.kpoplawski.repl.co/searchex/${search}`;
const artist = (artist) => `https://proxy.kpoplawski.repl.co/artist/${artist}`;

export default {
  search,
  searchex,
  artist
}