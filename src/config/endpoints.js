const endpoints = {
  search: (search) => `https://proxy.kpoplawski.repl.co/search/${search}`,
  searchex: (search) => `https://proxy.kpoplawski.repl.co/searchex/${search}`,
  artist: (artist) => `https://proxy.kpoplawski.repl.co/artist/${artist}`
}

export default endpoints;